emulator = mumu ; which emulator are you using (only mumu currently supported)
boss = carbuncle ; name of the boss (most current boss is carbuncle)

use_rw = 1 ; should the game search for rw? 1-yes 0-no
rw_name = sora_aasb ; name of the rw (musb, pusb, dvg, hmg, etc.)

restart_emulator = 1 ; should the game restart after doing a certain number of runs? 1-yes 0-no
restart_number = 50 ; how many runs should the game do before restarting

characters = locke, sora, ramza, cid_raines, vayne

char_orders1 = sb, ab1, ab1, ab1, ab1
char_orders2 = rw, ab1, ab1, ab1, ab1
char_orders3 = sb, ab1, ab1, ab1, ab1
char_orders4 = def
char_orders5 = def
