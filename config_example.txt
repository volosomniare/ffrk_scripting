emulator = mumu ; which emulator are you using (mumu, nox, and memu supported)
boss = sacred_minotaur ; name of the boss (current boss is greg)

use_rw = 0 ; should the game search for rw? 1-yes 0-no
rw_name = musb ; name of the rw (musb, pusb, dvg, hmg, etc.)
rw_preloaded = 0 ; have you filled your friends list with the rw? 1-yes 0-no

restart_emulator = 1 ; should the game restart emulator after doing a certain number of runs? 1-yes 0-no
restart_number = 50 ; how many runs should the game do before restarting nox

characters = braska, papalymo, rydia, terra, alphinaud

char_orders1 = ab1
char_orders2 = sb, ab1, ab1, ab1, ab1
char_orders3 = ab1
char_orders4 = ab1
char_orders5 = sb, ab1, ab1, ab1, ab1